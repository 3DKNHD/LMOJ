# Máximo en ventana

Arreglo $a$ y ventana de ancho $k$. Para cada $i=1..n-k+1$ imprime $\max(a_i,\dots,a_{i+k-1})$.
Deque $O(n)$.

## Entrada

$n$ $k$ ($1 \le k \le n \le 2 \cdot 10^5$).

$n$ enteros $a_i$ ($-10^9 \le a_i \le 10^9$).

## Salida

$n-k+1$ enteros en una línea.
