# Del S al E

Grilla $n \times m$: `S` inicio, `E` fin, `.` libre, `#` muro. Pasos 4-conectados.
Distancia mínima en pasos, o $-1$. BFS, no DFS.

## Entrada

$n$ $m$ ($1 \le n,m \le 1000$).

$n$ líneas de $m$ caracteres. Exactamente un `S` y un `E`.

## Salida

Un entero.
