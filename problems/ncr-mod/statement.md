# Combinatoria módulo

$T$ consultas $C(n,k) \bmod 10^9+7$. $C(n,k)=0$ si $k>n$ o $k<0$ (aquí $k\ge 0$).
$C(0,0)=1$. Precalcula factoriales e inversos hasta $10^6$.

## Entrada

$T$ ($1 \le T \le 10^5$).

$T$ líneas $n$ $k$ ($0 \le n,k \le 10^6$).

## Salida

$T$ líneas.
